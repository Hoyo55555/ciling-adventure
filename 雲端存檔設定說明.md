# 雲端存檔設定說明（約 5 分鐘）

設定完成後，學生打開遊戲會先看到「登入班級帳號」：輸入**班級、座號、4 位數字密碼**就能玩。
存檔會自動存進老師的 Google 試算表，換電腦只要重新登入就能繼續；老師也能在試算表上直接看到全班的等級、答題數與答對率。

> 不設定也能玩：存檔只會留在學生那台電腦的瀏覽器裡。

## 步驟

1. **建立試算表**：到 Google 雲端硬碟新增一個 Google 試算表，命名為「詞靈冒險存檔」。
2. **開啟 Apps Script**：在試算表選單點「擴充功能 → Apps Script」。
3. **貼上程式**：把編輯器裡原本的內容全部刪掉，貼上本專案 `gas/Code.gs` 的全部內容，按「儲存」。
4. **部署**：右上角「部署 → 新增部署作業」
   - 類型選「網頁應用程式」
   - 執行身分：**我**
   - 誰可以存取：**所有人**
   - 按「部署」，第一次會要求授權，依畫面允許即可（畫面若出現「Google 尚未驗證這個應用程式」，點「進階 → 前往（不安全）」，這是你自己的程式）。
5. **複製網址**：部署完成會得到一串 `https://script.google.com/macros/s/……/exec` 的網址。
6. **填進遊戲**：打開 `js/config.js`，把網址貼在 `cloudUrl: ''` 的引號中。
   （或把網址交給 Claude，由 Claude 填入並推送到 GitHub。）

完成後，可在遊戲標題畫面右上角的「教師設定 → 測試連線」確認是否成功。

## 常見問題

- **學生忘記密碼**：在 Apps Script 編輯器中打開 `resetPin` 函式，修改學生帳號（例如 `801班12號`）與新密碼後按「執行」。
- **查看全班進度**：打開試算表的「存檔」工作表，每位學生一列，有名字、世界觀、等級、章節、徽章數、答題數、答對率、錯題數與遊玩時間。存檔內容欄位預設隱藏。
- **修改了 Code.gs 之後**：要到「部署 → 管理部署作業 → 編輯 → 版本選『新版本』→ 部署」，網址不會改變。
- **安全性**：密碼以雜湊方式存放；這是給課堂使用的簡易帳號，請勿讓學生使用重要的密碼。
